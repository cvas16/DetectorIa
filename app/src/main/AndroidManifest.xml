<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.securepass.vision">

    <uses-feature android:name="android.hardware.camera" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.CAMERA" />

    <queries>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
    </queries>

    <application
        android:name="androidx.multidex.MultiDexApplication"
        android:icon="@drawable/ic_launcher_lion"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="ica" />

        <!-- ACTIVIDAD PRINCIPAL (Chooser de Kotlin) -->
        <activity
            android:name=".ui.activities.ChooserActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ACTIVIDADES DE KOTLIN -->
        <activity
            android:name=".ui.activities.LivePreviewActivity"
            android:exported="true" />
        <activity
            android:name=".ui.activities.CameraXLivePreviewActivity"
            android:exported="true" />

        <activity
            android:name=".ui.activities.HistoryActivity"
            android:exported="false"
            android:label="@string/history_title"
            android:theme="@style/AppTheme" />

        <!-- AJUSTES -->
        <activity
            android:name=".ui.activities.SettingsActivity"
            android:exported="false" />
    </application>

</manifest>
